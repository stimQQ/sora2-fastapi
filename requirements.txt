# Core Framework - Essential for FastAPI
fastapi==0.115.0
pydantic==2.9.2
pydantic-settings==2.5.2
httpx==0.27.0
python-dotenv==1.0.1
python-multipart==0.0.9
email-validator==2.2.0

# Database (PostgreSQL) - Async only, minimal footprint
sqlalchemy==2.0.35
asyncpg==0.30.0

# Redis (Async only)
redis==5.1.0

# Authentication & Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
authlib==1.3.2

# Google OAuth
google-auth==2.34.0
google-auth-oauthlib==1.2.1

# Payment - Stripe only (lighter than WeChat Pay)
stripe==10.12.0

# Storage - OSS only (lighter than boto3)
oss2==2.18.6

# Monitoring - Minimal
structlog==24.4.0

# EXCLUDED FOR VERCEL (run separately or use managed services):
# - uvicorn (Vercel provides ASGI server)
# - celery, flower (use external worker or Vercel Cron)
# - alembic (run migrations locally or in CI/CD)
# - psycopg2-binary (not needed with asyncpg)
# - boto3 (using OSS instead)
# - wechatpayv3 (complex deps, use Stripe)
# - geoip2, maxminddb (large DB files, use Cloudflare headers)
# - prometheus-client (use Vercel Analytics)
# - alibabacloud-dysmsapi (SMS not critical for API)
# - Testing tools (pytest, etc. - dev only)
# - Development tools (black, mypy, etc. - dev only)
